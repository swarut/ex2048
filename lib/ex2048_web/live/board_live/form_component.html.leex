<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "board-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :size %>
  <%= number_input f, :size %>
  <%= error_tag f, :size %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
